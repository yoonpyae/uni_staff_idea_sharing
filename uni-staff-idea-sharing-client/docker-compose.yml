version: '3.8'

services:
  web:
    build: .
    ports:
      - "2009:80"      # host 2007 -> container 80
    environment:
      - NODE_ENV=production
    restart: unless-stopped
    networks:
      - webnet

networks:
  webnet:
    driver: bridge